describe("Cart module/class", function() {
  
  var Cart = modulejs.require('cart');
  
  it("contains spec with an expectation", function() {
     expect(Cart.open()).toBe(undefined);
  });
  
  
  
});