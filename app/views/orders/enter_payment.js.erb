$('.wizard-step').removeClass('active').addClass('inactive');
$('.main-content').append("<%= j render(partial: 'enter_payment') %>");
$("form").parsley();
var payments = modulejs.require('payments');
payments.setup();
